def first_or_none(items: list[int]) -> int | None:
    """@generate
    behavior:
      return items[0] if items else None

    examples:
      first_or_none([1, 2, 3]) == 1
      first_or_none([42]) == 42

    constraints:
      no_imports
    """
    ...
