def add(a: int, b: int) -> int:
    """@generate
    behavior:
      require a > 0
      require c > 0
      return a + b

    examples:
      add(1, 2) == 3

    constraints:
      no_imports
    """
    ...
