def absolute(n: int) -> int:
    """@generate
    behavior:
      return -n if n < 0 else n

    examples:
      absolute(-5) == 5
      absolute(-5) == 10
      absolute(3) == 3

    constraints:
      no_imports
    """
    ...
