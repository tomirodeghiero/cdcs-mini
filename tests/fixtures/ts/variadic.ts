/**
 * @generate
 * behavior:
 *   return nums.length
 *
 * examples:
 *   sumAll(1, 2, 3) == 6
 *
 * constraints:
 *   no_imports
 */
export function sumAll(...nums: number[]): number {
  return 0;
}
