/**
 * @generate
 * behavior:
 *   require a > 0
 *   require c > 0
 *   return a + b
 *
 * examples:
 *   add(1, 2) == 3
 *
 * constraints:
 *   no_imports
 */
export function add(a: number, b: number): number {
  return 0;
}
