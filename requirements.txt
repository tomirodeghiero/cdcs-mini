# Vercel deploy shim. The authoritative dependency list lives in
# pyproject.toml; this file exists because the Vercel Python runtime resolves
# requirements.txt before pyproject.toml. Keep the two in sync.
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
pydantic>=2.0
rich>=13.0
.
